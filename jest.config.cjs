module.exports = {
    //TODO: jsdom
    //se instala con npm i --save-dev jest-environment-jsdom
    testEnvironment: 'jest-environment-jsdom',
    // útil configurando tests con jest
    setupFiles: ['./jest.setup.js'],
}